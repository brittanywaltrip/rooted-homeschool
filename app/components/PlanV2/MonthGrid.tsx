"use client";

// MonthGrid — 7-column month view. Composed of DayCell children.
// Built in Step 3. Currently a stub so the flag gate has something to render.

export default function MonthGrid() {
  return null;
}
